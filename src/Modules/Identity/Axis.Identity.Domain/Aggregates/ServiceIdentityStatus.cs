namespace Axis.Identity.Domain.Aggregates;

public enum ServiceIdentityStatus { Active, Revoked }
public enum ServiceIdentityKeyStatus { Active, Revoked }
public enum ServiceWorkspaceGrantStatus { Active, Revoked }
