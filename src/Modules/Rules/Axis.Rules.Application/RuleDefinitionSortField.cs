namespace Axis.Rules.Application;

public enum RuleDefinitionSortField
{
    Name,
    Origin,
    Status,
    ActiveVersion,
    LatestVersion,
    Revision,
    CreatedBy,
    CreatedAt,
    ModifiedBy,
    ModifiedAt,
}
